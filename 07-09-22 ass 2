# c-programs-#include <stdio.h>
int main() {
    int n,i,sum=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
    int *p;
    scanf("%d",&*p);
    sum=sum+*p;
    }
     printf ("%d",sum);
  return 0;
}
